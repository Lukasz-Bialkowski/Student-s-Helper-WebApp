package repository;

import entity.RemedialClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RemedialClassesRepository extends JpaRepository<RemedialClass, Long>{
}
